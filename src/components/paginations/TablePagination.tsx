import React from "react";

type TablePaginationProps = {
  prevData: () => void;
  nextData: () => void;
};
const TablePagination = ({ prevData, nextData }: TablePaginationProps) => {
  return <div></div>;
};

export default TablePagination;
